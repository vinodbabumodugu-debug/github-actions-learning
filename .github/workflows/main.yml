name: My Second GitHub Action

on:
  push:
    branches:
      - main

jobs:
  practice:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: echo "This is Step 1"

      - name: Step 2
        run: echo "This is Step 2"
